voi_mds:
  outputs:
    dev:
      type: postgres
      host: {{ env_var('DB_HOST') }}
      user: {{ env_var('DB_USER') }}
      password: {{ env_var('DB_PASSWORD') }}
      port: 5432
      dbname: stage_micromobility
      schema: public
  target: dev